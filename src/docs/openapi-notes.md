# Notas OpenAPI

La documentacion Swagger esta habilitada en:

- /api/docs

Las anotaciones OpenAPI se encuentran en:

- src/routes/adoption.router.js

Al iniciar la app (`npm start`), se puede navegar a:

- http://localhost:3000/api/docs
